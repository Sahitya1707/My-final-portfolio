const SkillModal = require("../../modals/skill");

const addSkill = (req, res) => {
  // TODO limit the skill to be one just one array
  console.log("add skill");

  console.log(req.body);
};

module.exports = addSkill;
